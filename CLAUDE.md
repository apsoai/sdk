# Claude Code Context

## NPM Package Namespace

**IMPORTANT**: The npm package namespace is `@apso`, NOT `@apsoai`.

- Correct: `@apso/sdk`
- Incorrect: `@apsoai/sdk`

This is despite the GitHub organization being `@apsoai`. Always use `@apso` for npm package references in documentation, imports, and install commands.
